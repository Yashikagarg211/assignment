# Copy this as the VITE_API_URL environment variable in your Netlify site settings.
# Replace with your actual Render backend URL (no trailing slash).
VITE_API_URL=https://flipkart-clone-backend.onrender.com/api
